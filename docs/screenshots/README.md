# Screenshots

Single hero image used by both READMEs:

| File | Purpose |
|------|---------|
| `screenshot.png` | Full app window (sidebar + chat + optional file preview) |

Replace `screenshot.png` to update the image in `README.md` / `README.zh-CN.md`.
